% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_shading.R
\name{create_shade_data}
\alias{create_shade_data}
\title{Create shade data for area under curve visualization}
\usage{
create_shade_data(segments, year = NULL)
}
\arguments{
\item{segments}{List of segment objects from segment_profile() (segmentation.R).
Each segment should be a list with:
\itemize{
  \item \code{is_positive} - Logical indicating if segment is above baseline
  \item \code{x} - Numeric vector of distances along transect
  \item \code{y} - Numeric vector of elevations
}}

\item{year}{Optional. Year value to include in output (for multi-year plots)}
}
\value{
List of dataframes (one per segment) with shade data, or NULL if no segments.
  Each dataframe contains:
  \itemize{
    \item \code{distance} - Numeric vector of x-coordinates
    \item \code{elevation} - Numeric vector of y-coordinates
    \item \code{year} - Year label (if provided)
  }
}
\description{
Converts pre-computed positive segments into dataframe format for ggplot shading.
Returns ALL segments instead of just the largest one.
}
\note{
Shades ALL regions where elevation >= 0 (supports multi-peak profiles)
}
