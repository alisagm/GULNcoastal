% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_filter.R
\name{apply_filter}
\alias{apply_filter}
\title{Apply filter to data}
\usage{
apply_filter(filter, data, auc_results = NULL, verbose = FALSE)
}
\arguments{
\item{filter}{data_filter object}

\item{data}{Dataframe with transect data (must have park, year, transect columns)}

\item{auc_results}{Optional dataframe with AUC results (filtered to match)}

\item{verbose}{Boolean. Print filtering summary. Default FALSE}
}
\value{
List with:
  - data: Filtered transect dataframe
  - auc_results: Filtered AUC dataframe (if provided)
  - resolved: The resolved_filter object with selection details
}
\description{
Resolves the filter and returns filtered dataframes. This is the main
function for actually filtering data using a data_filter object.
}
\examples{
\dontrun{
filter <- create_data_filter(year_selector = years_baseline_recent(2))
result <- apply_filter(filter, data, auc_results)
filtered_data <- result$data
filtered_auc <- result$auc_results
}
}
