% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_config.R
\name{validate_config}
\alias{validate_config}
\title{Validate configuration against available data}
\usage{
validate_config(config, data, verbose = TRUE)
}
\arguments{
\item{config}{plot_config object}

\item{data}{Dataframe with transect data}

\item{verbose}{If TRUE (default), print formatted report. If FALSE, return silently.}
}
\value{
validation_report object with is_valid, issues, and details
}
\description{
Checks that the data_filter can be resolved against the data and returns
valid selections. Returns a detailed validation report with issues and suggestions.
}
\examples{
\dontrun{
# Check config before running visualization
report <- validate_config(config, data)
if (!report$is_valid) {
  # Handle issues
}
}
}
